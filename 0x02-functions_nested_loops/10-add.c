#include "holberton.h"
/**
 * add - Function that adds two values
 * @a: first number
 * @b: second number
 * Return: returns sum of both numbers
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
